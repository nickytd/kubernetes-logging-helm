{{- if .Values.elasticsearch.in_cluster }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-data
  labels: {{ include "logging.labels" . | indent 4 }}
spec:
  ports:
  - name: transport
    port: 9300
    targetPort: transport
  selector: {{ include "logging.metadata.labels" . | indent 4 }}
    node: es
    type: data
  type: ClusterIP
{{- end }}